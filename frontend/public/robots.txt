# Если в ответе /robots.txt на проде есть строка Content-Signal — её добавляет Cloudflare Managed Content (не этот файл).
# Отключите вставку в панели Cloudflare, иначе валидаторы/Lighthouse могут помечать robots.txt как недействительный.
User-agent: *
Allow: /

Disallow: /api/
Disallow: /profile
Disallow: /cart
Disallow: /checkout
Disallow: /auth

# CSS/JS в /_next/ должны быть доступны поисковым роботам для рендеринга страниц.

# Sitemap
Sitemap: https://mudaroba.com/sitemap.xml
