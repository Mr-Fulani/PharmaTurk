# Mudaroba backend

Django/DRF backend for the Mudaroba marketplace. Dependency management is
authoritative in `pyproject.toml` and `poetry.lock`; use Poetry 2.4.1 and
Python 3.12.

Project-wide setup, operations, and architecture documentation lives in the
repository root and in `../docs/`.
